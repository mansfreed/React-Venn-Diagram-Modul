% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{load_csv}
\alias{load_csv}
\title{Load a delimited file (CSV/TSV) into a [`VennDataset-class`]}
\usage{
load_csv(path, binary = TRUE, delimiter = NULL, prefix_cols = 1L)
}
\arguments{
\item{path}{Path to the file.}

\item{binary}{`TRUE` for binary 0/1 mode (default), `FALSE` for aggregated.}

\item{delimiter}{Explicit delimiter override. `NULL` auto-detects from
`,`, `;`, tab, and space.}

\item{prefix_cols}{Number of leading metadata columns in binary mode
(default 1). Ignored when `binary = FALSE`.}
}
\value{
A [`VennDataset-class`].
}
\description{
Supports two layouts:
* Binary mode (default): one row per item, with 0/1 columns marking
  membership in each set. The first `prefix_cols` columns are item
  metadata; remaining columns are sets.
* Aggregated mode (`binary = FALSE`): each column is a set, and cells
  contain item identifiers. Empty cells are ignored.
}
\examples{
\dontrun{
ds <- load_csv("genes.csv", binary = TRUE)
ds@set_names
}
}
