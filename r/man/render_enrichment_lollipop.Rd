% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-enrichment-lollipop.R
\name{render_enrichment_lollipop}
\alias{render_enrichment_lollipop}
\title{Render the pairwise enrichment lollipop chart}
\usage{
render_enrichment_lollipop(
  result,
  metric = c("neglog10fdr", "foldEnrichment"),
  width = 560L,
  height = 240L
)
}
\arguments{
\item{result}{A [`RegionResult-class`] from [analyze()].}

\item{metric}{Stem-height metric -- `"neglog10fdr"` or `"foldEnrichment"`.}

\item{width, height}{Output SVG dimensions in pixels.}
}
\value{
An [`SvgImage-class`] with `content`, `width`, `height`.
}
\description{
Same data and significance scheme as [render_enrichment_bar()] but as a
stem-and-dot plot: a vertical line rises from the baseline to the metric
value, capped by a filled circle whose radius scales with
\code{sqrt(intersection / max_intersection)} (range 2.5--8 px).
}
\details{
Pure string construction -- no \pkg{xml2}.
}
\examples{
ds <- methods::new("VennDataset",
    set_names = c("A", "B"),
    items = list(A = c("x", "y"), B = c("y", "z")),
    item_order = c("x", "y", "z"),
    universe_size = 10L, source_path = NULL, format = "csv")
res <- analyze(ds)
img <- render_enrichment_lollipop(res)
nchar(slot(img, "content")) > 0
}
