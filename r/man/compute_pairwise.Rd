% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{compute_pairwise}
\alias{compute_pairwise}
\title{Compute all 5 pairwise statistical tables}
\usage{
compute_pairwise(
  set_names,
  inclusive_sizes,
  pairwise_intersections,
  universe_size
)
}
\arguments{
\item{set_names}{Ordered character vector of set identifiers (e.g. c("A","B","C")).}

\item{inclusive_sizes}{Named integer vector of inclusive set sizes (`names(inclusive_sizes)` matches `set_names`).}

\item{pairwise_intersections}{Named list of pair intersection counts. Keys are
"set_a|set_b" with set_a appearing earlier in `set_names` than set_b.}

\item{universe_size}{Hypergeometric universe N (population size). Integer >= 1.}
}
\value{
A [`StatisticsResult-class`] object.
}
\description{
Orchestrator that returns a [`StatisticsResult-class`] populated with
Jaccard, Dice, Overlap Coefficient, Fold Enrichment (square NxN matrices)
plus a long-form hypergeometric table with BH-FDR adjustment.
}
\examples{
compute_pairwise(
    set_names = c("A", "B"),
    inclusive_sizes = c(A = 10L, B = 8L),
    pairwise_intersections = list("A|B" = 5L),
    universe_size = 100L
)
}
