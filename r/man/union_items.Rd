% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region-accessors.R
\name{union_items}
\alias{union_items}
\title{Items in the union of the named sets}
\usage{
union_items(result, sets)
}
\arguments{
\item{result}{A [`RegionResult-class`] from [analyze()].}

\item{sets}{Character vector of set letters (`"A"`, `"B"`, ...) or display
names (values from `result@dataset@set_names`). May mix both.}
}
\value{
A character vector of item IDs (deduplicated, first-seen order).
}
\description{
Returns items that appear in ANY of the sets listed in `sets`. Equivalent
to `unique(c(items_in_A, items_in_B, ...))`.
}
\examples{
ds <- methods::new("VennDataset",
    set_names = c("A", "B", "C"),
    items = list(A = c("x", "y"), B = c("y", "z"), C = c("z", "w")),
    item_order = c("x", "y", "z", "w"),
    universe_size = 10L, source_path = NULL, format = "csv")
res <- analyze(ds)
union_items(res, c("A", "B"))   # "x", "y", "z"
}
