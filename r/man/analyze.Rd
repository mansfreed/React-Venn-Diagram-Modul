% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{analyze}
\alias{analyze}
\title{Analyze a Venn diagram dataset}
\usage{
analyze(dataset, model = "auto")
}
\arguments{
\item{dataset}{A [`VennDataset-class`] (from one of the `load_*` functions).}

\item{model}{Model identifier. `"auto"` picks the canonical model for the
dataset's set count (alphabetical first match), e.g. 4 sets ->
`venn-4-set`. `"proportional"` requests an area-proportional layout (only
supports 2-3 sets, added in Phase 3). Otherwise pass an explicit name from
[list_models()].}
}
\value{
A [`RegionResult-class`] with the per-region item membership, set
  sizes, and (lazily) `statistics(result)`.
}
\description{
Compute the Venn region map for a [`VennDataset-class`] and bind it to
a model.
}
\examples{
\dontrun{
ds <- load_sample("dataset_real_cancer_drivers_4")
result <- analyze(ds, model = "auto")
result@model
}
}
