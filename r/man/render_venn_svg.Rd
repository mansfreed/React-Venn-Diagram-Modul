% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-svg.R
\name{render_venn_svg}
\alias{render_venn_svg}
\title{Render a RegionResult onto its model SVG and return the raw SVG string}
\usage{
render_venn_svg(
  result,
  model = NULL,
  set_names = NULL,
  colors = NULL,
  title = NULL,
  show_names = TRUE,
  show_counts = TRUE
)
}
\arguments{
\item{result}{A [`RegionResult-class`].}

\item{model}{Optional model id override (filename stem). Default = `result@model`.}

\item{set_names}{Optional named character vector mapping letters (`"A"`, `"B"`, ...)
to display names. Unspecified letters fall back to `result@dataset@set_names`.}

\item{colors}{Optional named character vector mapping letters to fill hex colors.
Applies to `BulletX`, `ShapeX`, and `ShapeX2` (Euler extra shapes).}

\item{title}{Optional title override. If `NULL`, the template's default title
text is preserved.}

\item{show_names}{If `FALSE`, blanks every `NameA-I` element.}

\item{show_counts}{If `FALSE`, blanks every `Count_*` and `CountSUM_*` element.}
}
\value{
A `character` (length 1) with the raw SVG.
}
\description{
Loads the bundled SVG template for `result@model` (or the explicit `model`
override), walks the DOM via xml2 to overwrite text content (`Name*`,
`Count_*`, `CountSUM_*`, `Title`) and inline `fill:` colors (`Shape*`,
`Shape*2` for Euler extras, `Bullet*`), and serialises back to a string.
}
\details{
For `model = "proportional"`, delegates to [generate_proportional_svg()].

Mirrors Python `render_venn_svg` byte-for-byte except for: (a) the return
type is `character` instead of an `SvgImage` wrapper class; (b) xml2 may
emit slightly different whitespace/attribute ordering than lxml. Functional
content (text, fill colors, structure) is identical.
}
\examples{
\dontrun{
result <- analyze(load_sample("dataset_real_cancer_drivers_4"))
svg <- render_venn_svg(result)
writeLines(svg, "cancer_drivers.svg")
}
}
