% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{fold_enrichment}
\alias{fold_enrichment}
\title{Fold enrichment (observed / expected ratio)}
\usage{
fold_enrichment(N, K, n, k)
}
\arguments{
\item{N}{Population size (total items in the universe). Integer >= 1.}

\item{K}{Number of success states in the population (e.g. inclusive |A|). Integer >= 0.}

\item{n}{Number of draws (e.g. inclusive |B|). Integer >= 0.}

\item{k}{Observed successes (e.g. |A intersection B|). Integer >= 0.}
}
\value{
Numeric (>= 0; can exceed 1 for over-representation).
}
\description{
Computes (k * N) / (K * n). Returns 0.0 if any denominator is zero
(matches web tool convention).
}
\examples{
fold_enrichment(20000, 138, 581, 126)
}
