# @venn-diagram-lab/core

Pure, isomorphic core for Venn Diagram Lab: CSV/TSV/GMT/GMX parsing, region enumeration,
pairwise statistics, and TSV export. No DOM, no Node APIs — consumed by the web tool, the
`venn-diagram-lab` Node package, and (future) other JS runtimes. Internal package.
